# Patterns

These files provide reusable design guidance, not mandatory frameworks.

- Use a pattern only when it fits the current problem.
- Inspect the existing project before applying it.
- Prefer the smallest implementation that meets the milestone.
- Do not copy patterns blindly.
- Current project code and project documents take priority.

Patterns should clarify a design choice without forcing unrelated architecture into the project.
